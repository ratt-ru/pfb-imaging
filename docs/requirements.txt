# Documentation requirements for GitHub Pages
# These are the minimal requirements needed to build documentation

mkdocs>=1.5.0
mkdocs-material>=9.0.0
mkdocstrings[python]>=0.23.0
pymdown-extensions>=10.0.0

# Core dependencies needed for API documentation
numpy>=1.21.0
scipy>=1.7.0
jax>=0.4.31
dask>=2021.11.0
xarray>=0.20.0

# Minimal astronomy dependencies for docstring processing
# (avoid heavy dependencies like python-casacore in RTD environment)
astropy>=5.0.0